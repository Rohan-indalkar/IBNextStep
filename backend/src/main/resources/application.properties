spring.application.name=IBNextStep

# MongoDB

spring.data.mongodb.uri=${MONGODB_URI}

# Required for @Indexed/@CompoundIndex annotations (e.g. QuizAttempt's unique
# quizId+studentId index) to actually be created \u2014 Spring Data Mongo does not
# create indexes from annotations unless this is explicitly enabled.
spring.data.mongodb.auto-index-creation=true


# JWT
app.jwt.secret=8F9K2M7P1Q4R6T9V3X5Z8A2B7C1D4E6F9H2J5L8N1P4R7T0V3X6Z9A2B5C8D1E
app.jwt.expiration-ms=86400000


app.storage.upload-dir=./uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=700MB
spring.servlet.multipart.max-request-size=800MB

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rohanindalkar3767@gmail.com
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.push.vapid.public-key=${VAPID_PUBLIC_KEY}
app.push.vapid.private-key=${VAPID_PRIVATE_KEY}

app.push.vapid.subject=mailto:rohanindalkar3767@gmail.com



piston.base-url=${PISTON_BASE_URL:http://localhost:2000/api/v2}
piston.api-key=
piston.run-timeout-seconds=5
piston.compile-timeout-seconds=10


app.gemini.api-key=${GEMINI_API_KEY}
app.gemini.model=gemini-3.5-flash-lite


# Minimum AI overallScore (0-100) for a resume to be auto-approved.
# Below this, auto-review marks it NEEDS_CHANGES instead.
app.resume.ai.approval-threshold=70


# Attendance module
attendance.edit-window-days=7
attendance.warning-threshold-percentage=75


